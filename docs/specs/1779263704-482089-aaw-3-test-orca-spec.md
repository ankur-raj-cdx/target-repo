# Spec: [AAW-3] Test_Orca

**Request:** Add a GET /api/piing endpoint that returns { "pongg": true }
**Date:** 2024-01-10

## 1. Summary
Add a simple GET endpoint `/api/piing` that returns a fixed JSON response `{ "pongg": true }`. This provides a minimal test endpoint for pipeline validation and system health checks, similar to the existing `/api/yin` endpoint. The endpoint requires no authentication and has no external dependencies.

## 2. API Contract

| Method | Path | Auth | Request Body | Response | Status Codes |
|--------|------|------|--------------|----------|--------------|
| GET | `/api/piing` | none | N/A | `{ "pongg": true }` | 200 |

## 3. Data Model Changes
None.

## 4. Service Interactions
None. This endpoint is self-contained and does not interact with any services.

## 5. Feature Flag
None. This endpoint does not require a feature flag.

## 6. Error Handling

| Failure Scenario | Expected Behaviour | HTTP Status | Logged? |
|-----------------|-------------------|-------------|---------|
| N/A - no external dependencies | Always returns success | 200 | No |

## 7. Tests Required
- [ ] [happy_path] GET /api/piing returns 200 with body `{ "pongg": true }`
- [ ] [happy_path] GET /api/piing returns Content-Type: application/json header
- [ ] [regression] /api/status still returns 200 (health check not affected)
- [ ] [regression] Other endpoints like /api/yin continue to work

## 8. Rollback Plan
- Remove the route handler from routes/api.js
- No migrations or external dependencies to roll back

## 9. Out of Scope
- Adding authentication or authorization
- Making this endpoint depend on external services  
- Adding feature flags for this endpoint
- Adding request validation beyond basic Express routing
- Renaming to standard ping/pong spelling (keeping as piing/pongg)

## 10. Open Questions
- Is the unusual spelling 'piing' and 'pongg' intentional or should it be 'ping' and 'pong'?
- Should this endpoint be documented in the CLAUDE.md routes table?